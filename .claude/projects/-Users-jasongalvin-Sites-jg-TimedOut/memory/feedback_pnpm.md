---
name: Use pnpm not npm
description: User prefers pnpm as package manager, not npm
type: feedback
---

Use pnpm instead of npm for all package management commands.

**Why:** User preference — forgot to mention at project start but switched early.
**How to apply:** Use `pnpm install`, `pnpm run build`, etc. Never use npm or yarn.
